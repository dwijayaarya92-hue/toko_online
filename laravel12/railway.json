{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && php artisan serve --host=0.0.0.0 --port=$PORT",
    "preDeployCommand": "mkdir -p storage/database && touch storage/database/database.sqlite && php artisan migrate --force"
  }
}